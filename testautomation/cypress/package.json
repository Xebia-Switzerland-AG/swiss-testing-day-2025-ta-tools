{
  "name": "cypress",
  "version": "1.0.0",
  "description": "Test automation example with Cypress",
  "main": "index.js",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run --headed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "xebia",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^14.2.1",
    "typescript": "^5.2.2"
  }
}
